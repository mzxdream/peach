{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_win",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "./build_win.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}